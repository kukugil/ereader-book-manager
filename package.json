{
  "name": "ereader-book-transfer",
  "version": "1.0.0",
  "description": "Bluetooth MCU e-reader book transfer system",
  "main": "server/index.js",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "npm --prefix frontend install && cd frontend && rm -rf out && npx next build && cd .. && node -e \"var fs=require('fs');if(!fs.existsSync('frontend/out/index.html')){console.error('BUILD FAILED: index.html missing');process.exit(1)}\"",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "uuid": "^10.0.0",
    "better-sqlite3": "^11.7.0",
    "jszip": "^3.10.1",
    "sharp": "^0.33.5"
  }
}
