services:
  - type: web
    name: ereader-book-manager
    env: node
    buildCommand: cd frontend && npm install && npx next build
    startCommand: node server/index.js
    envVars:
      - key: NODE_VERSION
        value: 20.20.2
